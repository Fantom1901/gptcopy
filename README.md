# 🚀 gptcopy

**gptcopy** — консольная утилита на **Rust** для быстрой подготовки контекста проекта для LLM: ChatGPT, Claude, DeepSeek и других моделей.

Утилита сканирует проект, формирует его структуру и содержимое исходных файлов в единый текстовый контекст, после чего автоматически копирует результат в системный буфер обмена.

Основная задача `gptcopy` — быстро передать LLM только действительно полезную часть проекта без ручного копирования десятков файлов.

---

## ✨ Возможности

* ⚡ **Rust CLI** — нативный исполняемый бинарник без зависимости от shell-логики.
* 📂 **Сканирование проекта** — автоматически собирает дерево каталогов и содержимое текстовых файлов.
* 🚫 **Умное игнорирование** — пропускает служебные каталоги, зависимости, build-артефакты и lock-файлы.
* 🧩 **Поддержка `.gitignore`** — дополнительно учитывает локальные правила игнорирования проекта.
* 🌿 **Git-режим** — можно собрать только изменённые и новые файлы из Git.
* 🗜️ **Минификация** — удаляет пустые строки для уменьшения размера контекста.
* 🏷️ **XML-режим** — позволяет формировать контекст в XML-структуре, удобной для некоторых LLM.
* 🎯 **Фильтр расширений** — можно ограничить сбор только нужными типами файлов.
* 🔐 **Поиск потенциальных секретов** — предупреждает о возможных API-ключах, приватных ключах и токенах.
* 📝 **Определение текстовых файлов** — бинарные файлы автоматически пропускаются.
* 📋 **Автоматический буфер обмена** — поддерживаются `kitten`, `wl-copy` и `xclip`.
* 📊 **Статистика** — после выполнения показывает количество файлов, размер результата и приблизительное количество токенов.

---

## 🦀 Реализация

Проект переписан с shell-скрипта на **Rust**.

Основная логика реализована в одном CLI-бинарнике и не требует отдельного runtime.

Утилита самостоятельно выполняет:

```text
Аргументы CLI
     │
     ▼
Конфигурация
     │
     ├── Git mode
     ├── Minify
     ├── XML
     └── Extension filter
     │
     ▼
Сканирование проекта
     │
     ├── Ignore list
     ├── .gitignore
     └── Text-file detection
     │
     ▼
Чтение файлов
     │
     ├── Secret scan
     └── Formatting
     │
     ▼
Готовый контекст
     │
     ├── Clipboard
     │
     └── Console fallback
```

---

# 🛠 Установка

## Установка через GitHub Releases

Для установки используется официальный installer:

```bash
curl -sSL https://raw.githubusercontent.com/Fantom1901/gptcopy/main/install.sh | bash
```

Инсталлятор:

1. создаёт `$HOME/.local/bin`;
2. создаёт рабочую директорию `$HOME/scripts/gptcopy`;
3. получает последний GitHub Release;
4. скачивает актуальный бинарник;
5. создаёт ссылку `~/.local/bin/gptcopy`;
6. при необходимости добавляет `~/.local/bin` в `PATH`.

Поддерживаются:

* **Bash**
* **Zsh**
* **Fish**

Для Fish автоматически добавляется:

```fish
fish_add_path ~/.local/bin
```

Для Bash/Zsh добавляется соответствующий `PATH`.

После установки перезапустите терминал либо загрузите shell-конфигурацию командой, которую покажет installer.

---

## Удаление

Установку можно удалить через тот же installer:

```bash
curl -sSL https://raw.githubusercontent.com/Fantom1901/gptcopy/main/install.sh | bash -s -- --uninstall
```

Инсталлятор удаляет бинарник `gptcopy` и связанную с ним настройку `PATH`.

---

# 🚀 Использование

Общий синтаксис:

```text
gptcopy [пути] [флаги]
```

Если путь не указан, используется текущий каталог:

```bash
gptcopy
```

То же самое:

```bash
gptcopy .
```

---

## 📁 Собрать весь проект

```bash
gptcopy .
```

Утилита:

* строит дерево проекта;
* находит текстовые файлы;
* исключает игнорируемые каталоги и файлы;
* добавляет содержимое файлов;
* копирует результат в буфер обмена.

---

## 📂 Собрать конкретную директорию

```bash
gptcopy src/
```

Можно указывать несколько путей:

```bash
gptcopy src/ tests/ Cargo.toml
```

---

## 🗜️ Минификация

Флаг:

```bash
-m
```

или:

```bash
--minify
```

Удаляет пустые строки из содержимого файлов.

Пример:

```bash
gptcopy src/ -m
```

Это полезно, когда необходимо уменьшить размер контекста и количество токенов.

---

## 🌿 Только изменённые файлы Git

Флаг:

```bash
-c
```

или:

```bash
--changed
```

Использует Git и собирает:

* изменённые файлы;
* новые файлы;
* файлы, не отслеживаемые Git и не исключённые через стандартные Git ignore-правила.

Пример:

```bash
gptcopy -c
```

Особенно полезно при работе над уже существующим проектом:

```text
Проект
 ├── backend/
 ├── frontend/
 ├── docs/
 └── ...
       ↓
    gptcopy -c
       ↓
Только актуальные изменения
```

Если текущий каталог не является Git-репозиторием, в структуре будет выведено соответствующее сообщение.

---

## 🏷️ XML-режим

Флаг:

```bash
-x
```

или:

```bash
--xml
```

Формирует результат не в обычном Markdown-подобном представлении, а в XML-структуре.

Пример:

```bash
gptcopy src/ -x
```

Результат имеет структуру:

```xml
<project_structure>
...
</project_structure>

<source_code>
<file path="src/main.rs">
...
</file>

<file path="src/lib.rs">
...
</file>
</source_code>
```

XML-режим особенно удобен для моделей, которые хорошо работают со структурированным контекстом.

---

## 🎯 Фильтр по расширениям

Флаг:

```bash
-e
```

или:

```bash
--ext
```

Позволяет указать список расширений через запятую.

Например:

```bash
gptcopy . -e rs,toml
```

Будут обработаны только:

```text
*.rs
*.toml
```

Также поддерживается запись с точкой:

```bash
gptcopy . --ext .rs,.toml
```

Расширения сравниваются без учёта регистра.

Можно комбинировать фильтр с другими режимами:

```bash
gptcopy . -c -m -e rs,toml
```

---

# 🔧 Флаги

| Флаг | Полная форма   | Описание                                       |
| ---- | -------------- | ---------------------------------------------- |
| `-c` | `--changed`    | Обработать только изменённые и новые Git-файлы |
| `-m` | `--minify`     | Удалить пустые строки                          |
| `-x` | `--xml`        | Формировать контекст в XML                     |
| `-e` | `--ext <exts>` | Фильтр файлов по расширениям                   |
| `-h` | `--help`       | Показать справку                               |

---

# 📚 Примеры

### Весь проект

```bash
gptcopy .
```

### Исходники с минификацией

```bash
gptcopy src/ -m
```

### Только Rust-файлы

```bash
gptcopy . -e rs
```

### Rust + TOML

```bash
gptcopy . -e rs,toml
```

### Изменённые Git-файлы

```bash
gptcopy -c
```

### Изменённые файлы + минификация

```bash
gptcopy -c -m
```

### XML-контекст

```bash
gptcopy src/ -x
```

### Git + XML + минификация

```bash
gptcopy -c -m -x
```

### Несколько директорий

```bash
gptcopy src/ tests/
```

---

# 🚫 Игнорируемые файлы

Встроенный список исключений включает:

```text
node_modules
venv
.venv
.git
.idea
.vscode
__pycache__
target
dist
build
.next
.astro
.cache
package-lock.json
pnpm-lock.yaml
yarn.lock
```

Это позволяет не включать в LLM-контекст:

* зависимости;
* виртуальные окружения;
* IDE-конфигурацию;
* Git-метаданные;
* скомпилированные артефакты;
* framework cache;
* lock-файлы.

---

## 📄 Поддержка `.gitignore`

Помимо встроенного списка, `gptcopy` пытается прочитать:

```text
.gitignore
```

в корне переданного проекта.

Строки `.gitignore` используются как дополнительные имена для исключения.

Комментарии и пустые строки игнорируются.

Пример:

```gitignore
secrets
uploads
tmp
generated
```

После этого соответствующие элементы не попадут в собранный контекст.

> Текущая реализация `.gitignore` использует упрощённое сопоставление по имени файла или каталога, а не полноценный Git ignore parser.

---

# 🔐 Проверка потенциальных секретов

Перед добавлением содержимого файла `gptcopy` выполняет простую проверку на признаки случайно закоммиченных секретов.

В частности, проверяются конструкции вроде:

```text
BEGIN PRIVATE KEY
BEGIN RSA PRIVATE KEY
api_key
apikey
secret_key
aws_access_key_id
ghp_
```

При обнаружении совпадения выводится предупреждение:

```text
[ПРЕДУПРЕЖДЕНИЕ] Файл может содержать секреты/ключи!
```

### Важно

Это **не полноценный security scanner**.

Утилита только предупреждает о потенциально опасных строках и не блокирует копирование файла.

Перед отправкой контекста в стороннюю LLM всегда проверяйте содержимое самостоятельно.

---

# 📄 Определение бинарных файлов

`gptcopy` не пытается читать бинарные файлы как обычный текст.

Для предварительной проверки используется небольшой фрагмент файла. Если в нём обнаруживается нулевой байт, файл считается бинарным и пропускается.

Пустые файлы считаются текстовыми.

Это позволяет безопаснее работать с проектами, содержащими изображения, архивы, бинарники и другие не-текстовые данные.

---

# 📋 Буфер обмена

После формирования контекста утилита автоматически ищет доступный способ копирования.

Порядок проверки:

```text
kitten
   ↓
wl-copy
   ↓
xclip
```

## Kitty

Если терминал сообщает:

```text
TERM=xterm-kitty
```

и доступна команда:

```bash
kitten
```

используется:

```bash
kitten clipboard
```

## Wayland

При наличии:

```bash
wl-copy
```

используется системный Wayland clipboard.

Установка в большинстве Linux-дистрибутивов предоставляется пакетом `wl-clipboard`.

## X11

При наличии:

```bash
xclip
```

используется X11 clipboard.

---

## 💻 Работа без clipboard-утилиты

Если ни один поддерживаемый способ копирования не найден, `gptcopy` не завершается с ошибкой.

Вместо этого готовый контекст выводится непосредственно в терминал.

То есть даже без `wl-copy`, `xclip` или `kitten` утилита остаётся рабочей.

---

# 📊 Статистика

После завершения `gptcopy` выводит статистику:

```text
:: Готово! Обработано файлов: 24 | Размер: 182.4 KB | ~Tokens: 46694
```

Показываются:

* количество обработанных файлов;
* размер сформированного контекста;
* приблизительное количество токенов.

Оценка токенов является приближённой и рассчитывается на основе размера текста:

```text
tokens ≈ bytes / 4
```

Это не соответствует точному tokenizer конкретной LLM.

---

# 🧠 Формат обычного контекста

Без `--xml` структура результата выглядит примерно так:

```text
=== PROJECT STRUCTURE ===

project/
├── src
│   ├── main.rs
│   └── lib.rs
├── Cargo.toml
└── README.md

=== SOURCE CODE ===

FILE: src/main.rs
```

Содержимое файла

```

FILE: Cargo.toml
```

Содержимое файла

```text
```

Таким образом, LLM получает одновременно:

1. структуру проекта;
2. список файлов;
3. содержимое каждого файла.

---

# ⚙️ Поведение `--changed`

При использовании:

```bash
gptcopy -c
```

структура проекта и набор файлов работают немного иначе, чем при обычном сканировании.

Для получения состояния Git используется:

```bash
git status -s
```

А список файлов для обработки получается через:

```bash
git ls-files -m -o --exclude-standard
```

Это позволяет получить актуальные изменения, не включая стандартные Git-исключения.

---

# 🧩 Типичный workflow

Обычная работа с проектом:

```bash
# Внести изменения
vim src/main.rs

# Передать LLM только актуальные изменения
gptcopy -c
```

Для крупного проекта:

```bash
gptcopy -c -m
```

Для анализа конкретного технологического стека:

```bash
gptcopy . -e rs,toml,md
```

Для структурированного XML-контекста:

```bash
gptcopy -c -x
```

Комбинация всех режимов:

```bash
gptcopy -c -m -x -e rs,toml
```

---

# 🖥️ Поддерживаемые окружения

Утилита рассчитана на Unix-подобные окружения, где доступны стандартные инструменты и один из поддерживаемых clipboard backends.

Практический сценарий использования:

* Linux + Wayland;
* Linux + X11;
* терминал Kitty;
* Bash;
* Zsh;
* Fish;
* Git-репозитории.

Сам `gptcopy` является нативным Rust-бинарником.

---

# 📦 Архитектура

Утилита реализована без внешнего CLI-фреймворка и самостоятельно разбирает аргументы командной строки.

Основные компоненты:

| Компонент                 | Назначение                              |
| ------------------------- | --------------------------------------- |
| `Config`                  | Хранение параметров запуска             |
| `IGNORE_LIST`             | Встроенный список исключений            |
| `SECRET_PATTERNS`         | Шаблоны поиска потенциальных секретов   |
| `load_gitignore()`        | Загрузка локального `.gitignore`        |
| `is_text_file()`          | Определение текстового файла            |
| `collect_files()`         | Рекурсивный сбор файлов                 |
| `print_tree()`            | Формирование дерева проекта             |
| `get_clipboard_process()` | Определение clipboard backend           |
| `scan_for_secrets()`      | Предупреждение о потенциальных секретах |

---

# 🔒 Безопасность

`gptcopy` предназначен для подготовки контекста, а не для хранения или защиты секретов.

Учитывайте несколько моментов:

* содержимое файлов читается локально;
* потенциальные секреты только обнаруживаются и помечаются предупреждением;
* результат может содержать чувствительные данные;
* после копирования содержимое находится в системном буфере обмена;
* перед отправкой контекста в LLM необходимо проверить результат.

Не используйте `gptcopy` без проверки на проектах, содержащих:

```text
.env
credentials
private keys
API tokens
SSH keys
production configuration
```

---

# 🧪 Справка

Получить список доступных параметров:

```bash
gptcopy --help
```

Вывод включает:

```text
🚀 gptcopy — Подготовка контекста проекта для LLM (Rust edition)

ИСПОЛЬЗОВАНИЕ:
  gptcopy [пути] [флаги]

ФЛАГИ:
  -c, --changed       Копировать только изменённые файлы (нужен Git)
  -m, --minify        Удалить пустые строки из кода
  -x, --xml           Форматировать контекст в XML
  -e, --ext <exts>    Фильтр по расширениям через запятую
  -h, --help          Показать эту справку
```

---

# 📌 Быстрая шпаргалка

```bash
# Весь проект
gptcopy .

# Конкретная папка
gptcopy src/

# Только изменения
gptcopy -c

# Только изменения + минификация
gptcopy -c -m

# XML
gptcopy -x

# Только Rust и TOML
gptcopy . -e rs,toml

# Несколько путей
gptcopy src/ tests/

# Максимально полезный контекст для правок
gptcopy -c -m -x
```

---
